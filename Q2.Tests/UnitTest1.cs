using Moq;

namespace Q2.Tests
{
    public class UnitTest1
    {
        //[Fact]
        //public void CreateCustomer_ShouldCreateCustomer()
        //{
        //    // Arrange
        //    var mockRepository = new Mock<ICustomerProfileRepository>();
        //    var customerService = new CreateModel(mockRepository.Object);
        //    var customer = new Customer { Name = "John Doe" };

        //    // Act
        //    var result = customerService.CreateCustomer(customer);

        //    // Assert
        //    mockRepository.Verify(repo => repo.Create(customer), Times.Once);
        //    Assert.True(result); // Check if the result indicates success
        //}
    }
}